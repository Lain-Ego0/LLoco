"""Legacy Go2 configuration modules.

Task registration is owned by the external ``lainloco`` package through the
``mjlab.tasks`` entry point. These modules remain temporarily importable so
existing checkpoints and class paths continue to load during the migration.
"""
