# 动作资产库

工作台统一从本目录枚举 BVH、PKL、CSV、NPZ、TXT。

- g1/、g1_23dof/、go2_amp/：已有项目动作。
- converted/：归档的已转换训练动作。
- examples/：开发验证片段，不是完整 BVH 数据集。
- imports/：通过界面导入的文件，使用独立子目录防止同名覆盖。
- generated/：转换结果，按机器人分类并自动生成唯一文件名。

在 Tracking 页点击“导入动作文件”，或在下拉框 / 资产表中选择已有文件。不需要手动填写输入和输出路径。
