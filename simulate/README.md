# Simulation bridge

The bridge source is kept here, while MuJoCo itself must be installed separately.
Point CMake at the system MuJoCo package, or provide `mujoco_DIR`, then build with a
standard out-of-tree CMake build.
